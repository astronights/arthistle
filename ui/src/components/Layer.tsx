import React from "react";
import NavBar from "./NavBar";
import Router from "./Router";

const Layer = () => {
  return (
    <div>
      <NavBar />
      <Router />
    </div>
  );
};

export default Layer;
